'use client';

/**
 * next/image needs `loaderFile` to be a path inside the app, so this is a
 * one-line re-export of the shared implementation.
 */
export { default } from '@blog/contentful/image-loader';
